/*
 * Root layout за Menu проекта. Този компонент дефинира
 * основната HTML структура и включва глобалните стилове
 * и хедъра за навигация. Чрез разполагане на <Header />
 * тук гарантираме, че навигацията присъства на всяка страница.
 *
 * Ако в бъдеще се добави глобален контекст (например за
 * продуктови категории), той може да бъде декориран около
 * "children" или около <Header />. Засега навигацията е
 * статична.
 */

import './globals.css';
import Header from '../components/Header';

// Мета данни за цялото приложение. Те могат да бъдат
// надписани в отделните страници, ако е необходимо.
export const metadata = {
  title: 'Menu Project',
  description: 'Онлайн система за управление на меню с поддръжка на категории и рецепти.',
};

export default function RootLayout({ children }) {
  return (
    <html lang="bg">
      <body>
        {/* Основен хедър с навигация */}
        <Header />
        {/* Main контейнер за съдържанието на страниците */}
        <main>
          {children}
        </main>
      </body>
    </html>
  );
}